/**
 * Moved to `@/Themes/contract/runtime/JsonLd` in Phase 7.2 — it is a public
 * theme export, so it has to live inside what the `flowcms` package compiles.
 * Re-exported here so the app's own call sites are unchanged.
 */
export { default } from "@/Themes/contract/runtime/JsonLd"
